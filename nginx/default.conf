server {
    listen       80;
    server_name  172.203.97.190;
    root   /app/dashboard/;

    location /dashboard/ {
        alias /app/dashboard/;
        index  index.html;
        try_files $uri $uri/ /dashboard/index.html;
    }
}
